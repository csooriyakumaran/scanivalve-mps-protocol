# Features


# Hardening
- [ ] Add `MPS_IMPLEMENTATION` guard
- [ ] Add `mps_parse_*(uint8_t* bytes, size_t len, MpsXxxPacket* out)` to handle (de)serialization of byte streams (e.g., from network, or file)
